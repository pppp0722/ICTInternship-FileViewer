const initialState = {
    url: 'http://localhost:8091'
    // url: '121.165.105.124:8091'
};

const url = (state = initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
};

export default url;